# =============================================================================
# DDM501 Lab 3 - Testing & CI/CD
# Runtime dependencies (Python 3.10+)
#
# scikit-surprise is 1.1.5, not the starter's 1.1.3: 1.1.3 has no Python 3.11
# wheel and its C extension fails to build against modern numpy headers, which
# breaks both local installs and the CI runner.
# =============================================================================

# FastAPI and server
fastapi>=0.104.1
uvicorn>=0.24.0

# Data processing
pandas>=2.1.3
numpy>=1.26.2

# Machine Learning
scikit-learn>=1.3.2
scikit-surprise==1.1.5

# Validation
pydantic>=2.5.2

# Configuration
python-dotenv>=1.0.0
